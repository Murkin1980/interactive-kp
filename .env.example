NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only target backend. Keep `supabase` until IKP-MB5 cutover is accepted.
DATA_BACKEND=supabase
MINIBASE_URL=
# Never rename this to NEXT_PUBLIC_* or expose it to browser code.
MINIBASE_SECRET_KEY=
