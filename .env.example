NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MiniBase is the only target data backend. Supabase variables above are kept
# temporarily for the existing manager-session verifier during auth cutover.
DATA_BACKEND=minibase
MINIBASE_URL=
# Never rename this to NEXT_PUBLIC_* or expose it to browser code.
MINIBASE_SECRET_KEY=
